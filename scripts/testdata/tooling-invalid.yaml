# Fixture tooling: config válida salvo tooling.gentle_ai (una rama, no una versión).
sdaf:
  version: "0.4.0"

project:
  name: FixtureTooling
  language: es

stack:
  pack: null

agents:
  active:
    - specification
  stubs: []

tooling:
  gentle_ai: main
