# Fixture T1: config válida con una release de gentle-ai que incluye sdd (aviso, no error).
sdaf:
  version: "0.4.0"

project:
  name: FixtureToolingSdd
  language: es

stack:
  pack: null

agents:
  active:
    - specification
  stubs: []

tooling:
  gentle_ai: "3.7.0"
